name: E-commerce Quality Lab CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Шаг 1: Получить код из репозитория
      - name: Checkout code
        uses: actions/checkout@v4

      # Шаг 2: Показать структуру файлов
      - name: Debug - Show directory structure
        run: |
          echo "📁 Структура репозитория:"
          pwd
          ls -la
          echo ""
          echo "🔍 Проверка наличия папок:"
          [ -d "02-local-mock-nopcommerce" ] && echo "✅ 02-local-mock-nopcommerce существует" || echo "❌ 02-local-mock-nopcommerce не найден"

      # Шаг 3: Установить Java 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # Шаг 4: Установить Docker Compose (правильный способ)
      - name: Setup Docker Compose
        run: |
          echo "🐳 Установка Docker Compose..."
          
          # Скачиваем docker-compose бинарник
          sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          
          # Даем права на выполнение
          sudo chmod +x /usr/local/bin/docker-compose
          
          # Проверяем установку
          docker-compose --version

      # Шаг 5: Запустить nopCommerce через Docker Compose
      - name: Start nopCommerce
        run: |
          echo "🚀 Запуск nopCommerce..."
          cd 02-local-mock-nopcommerce
          
          # Используем docker-compose (через дефис)
          docker-compose up -d
          
          # Ждем запуск
          echo "⏳ Ожидание запуска контейнеров (60 секунд)..."
          sleep 60
          
          # Проверяем статус
          docker ps
          echo ""
          echo "🔍 Проверка доступности nopCommerce..."
          curl -I http://localhost:8080 && echo "✅ nopCommerce запущен!" || echo "⚠️ nopCommerce еще не готов"

      # Шаг 6: Запустить тесты
      - name: Run Tests
        run: |
          echo "🧪 Запуск тестов..."
          cd 02-local-mock-nopcommerce/ui-automation
          mvn clean test

      # Шаг 7: Остановить контейнеры
      - name: Stop containers
        if: always()
        run: |
          echo "🛑 Остановка контейнеров..."
          cd 02-local-mock-nopcommerce
          docker-compose down